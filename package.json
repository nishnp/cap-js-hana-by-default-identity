{
  "name": "cap-js-hana-by-default-identity",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/hana": "^1.9.0",
    "@sap/cds": "^8",
    "express": "^4",
    "patch-package": "^8.0.0"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.6",
    "@cap-js/sqlite": "^1"
  },
  "scripts": {
    "start": "DEBUG=sql cds watch --profile hybrid"
  }
}
